- name: Sway - Installed
  become: yes
  package:
    name: sway
    state: present

- name: Sway - Config Directory Exists
  file:
    path: ~/.config/sway
    state: directory

- name: Sway - Config File Sync
  copy:
    src: config
    dest: ~/.config/sway/config