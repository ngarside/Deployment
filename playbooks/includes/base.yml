- hosts: localhost
  pre_tasks:
    - name: Kernel
      include_tasks: ../../tasks/kernel.yml
  roles:
    - cleanup
    - update
    - flatpak
    - virtualbox-guest
    - spotify
    - background
    - firefox
    - libreoffice
    - libreoffice-calc
    - libreoffice-writer
    - papirus
    - systemd
    - dropbox
    - gnome-clocks
    - gnome-weather
    - gnome-podcasts
    - ssh
